# {{ title }} 

{% for package in software %}
({{ package.id }})=
## [{{ package.name }}]({{package.url}})

{{ package.description }}

**Categories:** {{ package.category | join(", ") }}  
{% if package.pmcid -%}
**Publication:** [{{ package.pmcid }}](https://pmc.ncbi.nlm.nih.gov/articles/{{ package.pmcid }}/)
{% endif %}

{% endfor %}
